name = "fast-task-executor"
description = "Routine, bounded PNMF implementation."
model = "gpt-5.6-terra"
model_reasoning_effort = "medium"
sandbox_mode = "workspace-write"

developer_instructions = """
Follow root and scoped AGENTS.md. Implement routine bounded changes from
reconnaissance evidence, run focused tests, and return a concise summary.
"""
